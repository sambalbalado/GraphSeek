# Benchmarks

Benchmark code will be added in the roadmap. Generated datasets and results
are ignored by Git by default. Publish only small, final result tables with the
hardware, software versions, random seed, and exact command used.

